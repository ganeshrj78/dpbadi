# Local Development - copy this to .env and customize

# Secret key for Flask sessions (generate a random string for production)
SECRET_KEY=dev-secret-key-change-in-production

# Database URL (leave empty for SQLite, or set PostgreSQL URL)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Admin password for the application
APP_PASSWORD=bpbadi2024

# Set to 'true' when deployed on Render
# RENDER=true
